{
  "name": "git-dat-vst",
  // ... other fields ...
  
  // ADD THIS (Replace with your actual URL):
  "homepage": "https://<your-username>.github.io/<repo-name>",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    // ADD THIS (Optional, but makes deploying instant):
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  // ... dependencies ...
}
